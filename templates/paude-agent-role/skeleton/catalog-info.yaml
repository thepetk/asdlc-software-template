apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.name }}
  description: ${{ values.title }}
  annotations:
    backstage.io/techdocs-ref: dir:.
spec:
  type: paude-agent-role
  owner: ${{ values.owner }}
  lifecycle: experimental
  title: ${{ values.title }}
  agentType: ${{ values.agentType }}
  systemPrompt: ${{ values.systemPrompt }}
