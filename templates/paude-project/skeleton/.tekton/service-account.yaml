apiVersion: v1
kind: ServiceAccount
metadata:
  name: ${{ values.name }}-tekton-triggers-sa
  namespace: ${{ values.argoNS }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: ${{ values.name }}-tekton-triggers-role
  namespace: ${{ values.argoNS }}
rules:
  - apiGroups: ["triggers.tekton.dev"]
    resources: ["eventlisteners", "triggerbindings", "triggertemplates", "triggers"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["tekton.dev"]
    resources: ["pipelineruns"]
    verbs: ["create"]
  - apiGroups: [""]
    resources: ["configmaps", "secrets"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: ${{ values.name }}-tekton-triggers-rolebinding
  namespace: ${{ values.argoNS }}
subjects:
  - kind: ServiceAccount
    name: ${{ values.name }}-tekton-triggers-sa
    namespace: ${{ values.argoNS }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: ${{ values.name }}-tekton-triggers-role
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: ${{ values.name }}-tekton-clusterinterceptors-role
rules:
  - apiGroups: ["triggers.tekton.dev"]
    resources: ["clusterinterceptors"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: ${{ values.name }}-tekton-clusterinterceptors-rolebinding
subjects:
  - kind: ServiceAccount
    name: ${{ values.name }}-tekton-triggers-sa
    namespace: ${{ values.argoNS }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: ${{ values.name }}-tekton-clusterinterceptors-role
