apiVersion: v1
kind: Service
metadata:
  name: ${{ values.name }}
spec:
  type: ClusterIP
  selector:
    app: paude
    paude.io/session-name: ${{ values.name }}
  ports:
    - name: web-ui
      port: 18789
      targetPort: 18789
      protocol: TCP
